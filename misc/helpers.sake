namespace :misc do
  task :helpers do
    def err(msg)
      abort(msg.gsub(/^ +/,'').strip)
    end

    class Object
      def returning(value)
        yield(value)
        value
      end
    end
  end
end